node 'host1' {
  connection  => local
  environment => 'staging'
  groups      = ['staging']
}

# Defaults and assigned groups are included automatically by Geppetto Server.
# Host-specific bits can still be included here.
include 'keys.fops'
